@model ClinicaVeterinaria.Models.RecoverPasswordViewModel
@{
    ViewData["Title"] = "RecoverPassword";
}

<h2>Recover Password</h2>

<hr />

<div class="row">
    <div class="col-md-4 offset-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly"></div>

            <div class="form-group">
                <label asp-for="Email">Email</label>
                <input asp-for="Email" class="form-control" />
                <span asp-validation-for="Email" class="text-warning"></span>
            </div>

            <div class="form-group">
                <button type="submit" value="Recover password" class="btn btn-primary"><i class="fas fa-unlock-alt"></i> Recover Password</button>
                <a asp-action="Login" class="btn btn-success"><i class="fas fa-angle-double-left"></i> Back to login</a>
            </div>
        </form>
        <div class="text-success">
            <p>
                @ViewBag.Message
            </p>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

